let values = [8, 1, 7, 4, 2, 9]


/* for (let pos=0; pos < values.length; pos++) {
    console.log(`The position ${pos} has the value ${values[pos]}`)
}
*/

// Simplified
for(let pos in num) {
    console.log(`The position ${pos} has value ${values[pos]}`)
}